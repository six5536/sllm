Double-check the whole plan (the `double-check` skill): go through every requirement, decision
and phase and confirm each is implemented, tested and traced (`@zen-impl` / `@zen-test`, ticked
acceptance criteria). Look for what the plan got wrong, not only what the code missed. Fix small
things directly and commit.

Fire clean when nothing is left, or issuesFound naming what needs real work.
