Set the plan's status to completed and add an Outcome section: one row per phase with its
commit, plus anything that changed from the plan while implementing. Commit it. Then tell the
user, in plain English, what was built, what changed from the plan, and what is waiting on them.
Ask before pushing.
